# Vercel 빌드에 들어가는 값. 비밀이 아니라서 커밋한다.
#
# 개발(`npm run dev`)에서는 이 값이 비어 있고, vite.config.js 의 프록시가
# /api 와 /ws 를 localhost:8000 으로 넘긴다. 배포본엔 프록시가 없으므로
# 백엔드 절대 주소가 필요하다.
VITE_API_BASE=https://pokemon-api.han-david.com
